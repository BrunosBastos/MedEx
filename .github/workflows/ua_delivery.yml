
name: Run Delivery

on:
  pull_request:
      branches:
          - main

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: docker login
      env:
        DOCKER_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
    - name: Run deployment
      run: |
        docker-compose up -d
